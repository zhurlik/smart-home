plugins {
    id 'com.github.zhurlik.arduino'
}

apply plugin: 'com.github.zhurlik.arduino'

ArduinoIde {
    homeDir "${projectDir}/arduino-${property('arduino.ide.version')}"
}

dependencies {
    arduinoIde "arduino:linux64:${property('arduino.ide.version')}@tar.xz"
}